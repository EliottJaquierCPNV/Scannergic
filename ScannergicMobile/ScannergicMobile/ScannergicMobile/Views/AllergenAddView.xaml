<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ScannergicMobile.Views.AllergenAddView"
             Title="{Binding Title}"
             xmlns:local="clr-namespace:ScannergicMobile.ViewModels"  
             xmlns:model="clr-namespace:ScannergicMobile.Models"
             x:Name="AllergicAddXML">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Retour" Command="{Binding ShowAllergicHomeViewCommand}"/>
    </ContentPage.ToolbarItems>
    <RefreshView x:DataType="local:AllergicAddViewModel" Command="{Binding LoadAllergensCommand}" IsRefreshing="{Binding IsBusy, Mode=TwoWay}">
        <ListView x:Name="ItemsListView"
                ItemsSource="{Binding Allergens}"
                SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:Allergen">
                    <TextCell Text="{Binding Name}" Command="{Binding BindingContext.AllergenTapped, Source={x:Reference AllergicAddXML} }" CommandParameter="{Binding .}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </RefreshView>
</ContentPage>